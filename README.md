# Cinzas

Santuário pessoal de jogos — front only (React + TypeScript + Tailwind).

```bash
npm install
npm run dev
```

Abre em `http://localhost:5173/`.

A lista de jogos fica em `src/data/games.ts`.
